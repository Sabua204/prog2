/*abc,def
.
*/


int main()
{
    int integer = 0x89;
   char num = 'h';
    char letter2 = 'i';
    char multipleLetters[] = "Hello, world!\x7E";// check remaining C escape characters "\xFF" // "a" = 97
    if (123 < 456) {
        integer++;
    } 
    else if (123 == 456) {
        integer--;
    }
    //And this is a comment! 
   // ++ -> INCREMENT, not PLUS PLUS
    //& && | ||
    return 0;
}
